[build-system]
requires = ["setuptools>=61.0", "setuptools_scm>=6.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ethopy-analysis"
dynamic = ["version"]
description = "Data analysis and visualization package for Ethopy behavioral experiments"
readme = "README.md"
license = {text = "MIT"}
authors = [
    {name = "Ethopy Analysis Contributors"}
]
keywords = ["neuroscience", "behavior", "data-analysis", "visualization", "ethopy"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Bio-Informatics",
    "Topic :: Scientific/Engineering :: Visualization",
]
requires-python = ">=3.9"
# Every runtime dependency carries an upper bound: this package talks to a
# DataJoint schema through APIs that have broken across majors before
# (datajoint 2.0), so majors are opt-in rather than automatic.
dependencies = [
    "datajoint>=0.14.0,<2.0.0",
    "matplotlib>=3.5.0,<4.0.0",
    "numpy>=1.20.0,<3.0.0",
    "pandas>=1.3.0,<3.0.0",
    "seaborn>=0.11.0,<0.14.0",
    "click>=8.0.0,<9.0.0",
    # datajoint 0.14.x does `import pkg_resources` at import time but does not
    # depend on setuptools itself. setuptools 81 dropped pkg_resources, so a
    # fresh install picks up setuptools>=81 and `import datajoint` dies with
    # ModuleNotFoundError. Cap it until datajoint stops using pkg_resources.
    "setuptools<81",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0,<9.0.0",
    "pytest-cov>=4.0.0,<7.0.0",
    "black>=22.0.0,<26.0.0",
    "isort>=5.10.0,<7.0.0",
    "flake8>=4.0.0,<8.0.0",
    "mypy>=0.950,<2.0.0",
    "jupyter>=1.0.0,<2.0.0",
    "ipykernel>=6.0.0,<7.0.0",
]
docs = [
    "mkdocs>=1.4.0,<2.0.0",
    "mkdocs-material>=8.0.0,<10.0.0",
    "mkdocstrings[python]>=0.19.0,<1.0.0",
]
# Jupyter kernel for the example notebooks; not needed by the library itself.
notebooks = [
    "ipykernel>=6.0.0,<7.0.0",
    "jupyter>=1.0.0,<2.0.0",
]

[project.urls]
Homepage = "https://github.com/ef-lab/ethopy_analysis"
Repository = "https://github.com/ef-lab/ethopy_analysis"
Documentation = "https://ef-lab.github.io/ethopy_analysis"
"Bug Tracker" = "https://github.com/ef-lab/ethopy_analysis/issues"

[project.scripts]
ethopy-analysis = "ethopy_analysis.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"ethopy_analysis" = ["*.json", "*.md"]

[tool.black]
line-length = 88
target-version = ['py39']
include = '\.pyi?$'
extend-exclude = '''
/(
  # directories
  \.eggs
  | \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | build
  | dist
)/
'''

[tool.isort]
profile = "black"
multi_line_output = 3
line_length = 88
known_first_party = ["ethopy_analysis"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py", "*_test.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "--strict-markers --strict-config --cov=ethopy_analysis"

[tool.mypy]
python_version = "3.9"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
warn_unreachable = true
strict_equality = true

[tool.setuptools_scm]
write_to = "src/ethopy_analysis/_version.py"
version_scheme = "post-release"
local_scheme = "no-local-version"